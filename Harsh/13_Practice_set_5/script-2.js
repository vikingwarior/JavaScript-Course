let num = parseInt(prompt(`Enter a number:`));

if (num > 4){
    // Can be used to alter the URL of a page and redirect it
    window.location = "https://github.com/vikingwarior" // or // location.href = "https://github.com/vikingwarior" can also be used
}

document.body.style.backgroundColor = prompt(`Enter a color:`);